# Plans

- Avoid time estimates
- Avoid estimates of difficulty
- Avoid extensive summary sections
- Avoid adding achievements
- Use concrete tasks with checkboxes
- Order tasks in an order in which they can be implemented
- When updating tasks after completion, remove code and large descriptions and replace with a concise summary of the work
- Omit the validation section
